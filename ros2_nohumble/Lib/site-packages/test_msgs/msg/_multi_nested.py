# generated from rosidl_generator_py/resource/_idl.py.em
# with input from test_msgs:msg\MultiNested.idl
# generated code does not contain a copyright notice

from __future__ import annotations

import collections.abc
import os
import typing

import rosidl_pycommon.interface_base_classes

if typing.TYPE_CHECKING:
    from ctypes import Structure

    class PyCapsule(Structure):
        pass  # don't need to define the full structure


# This is being done at the module level and not on the instance level to avoid looking
# for the same variable multiple times on each instance. This variable is not supposed to
# change during runtime so it makes sense to only look for it once.
ros_python_check_fields = os.getenv('ROS_PYTHON_CHECK_FIELDS', default='')


if typing.TYPE_CHECKING:
    import test_msgs.msg  # noqa: E402, I100, I201, I300


# Import statements for member types

import builtins  # noqa: E402, I100

import rosidl_parser.definition  # noqa: E402, I100


class Metaclass_MultiNested(rosidl_pycommon.interface_base_classes.MessageTypeSupportMeta):
    """Metaclass of message 'MultiNested'."""

    _CREATE_ROS_MESSAGE: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _CONVERT_FROM_PY: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _CONVERT_TO_PY: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _DESTROY_ROS_MESSAGE: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _TYPE_SUPPORT: typing.ClassVar[typing.Optional[PyCapsule]] = None

    class MultiNestedConstants(typing.TypedDict):
        pass

    __constants: MultiNestedConstants = {
    }

    @classmethod
    def __import_type_support__(cls) -> None:
        try:
            from rosidl_generator_py import import_type_support  # type: ignore[attr-defined]
            module = import_type_support('test_msgs')
        except ImportError:
            import logging
            import traceback
            logger = logging.getLogger(
                'test_msgs.msg.MultiNested')
            logger.debug(
                'Failed to import needed modules for type support:\n' +
                traceback.format_exc())
        else:
            cls._CREATE_ROS_MESSAGE = module.create_ros_message_msg__msg__multi_nested
            cls._CONVERT_FROM_PY = module.convert_from_py_msg__msg__multi_nested
            cls._CONVERT_TO_PY = module.convert_to_py_msg__msg__multi_nested
            cls._TYPE_SUPPORT = module.type_support_msg__msg__multi_nested
            cls._DESTROY_ROS_MESSAGE = module.destroy_ros_message_msg__msg__multi_nested

            from test_msgs.msg import Arrays
            if Arrays._TYPE_SUPPORT is None:
                Arrays.__import_type_support__()

            from test_msgs.msg import BoundedSequences
            if BoundedSequences._TYPE_SUPPORT is None:
                BoundedSequences.__import_type_support__()

            from test_msgs.msg import UnboundedSequences
            if UnboundedSequences._TYPE_SUPPORT is None:
                UnboundedSequences.__import_type_support__()

    @classmethod
    def __prepare__(metacls, name: str, bases: tuple[type[typing.Any], ...], /, **kwds: typing.Any) -> collections.abc.MutableMapping[str, object]:
        # list constant names here so that they appear in the help text of
        # the message class under "Data and other attributes defined here:"
        # as well as populate each message instance
        return {
        }


class MultiNested(rosidl_pycommon.interface_base_classes.BaseMessage, metaclass=Metaclass_MultiNested):
    """Message class 'MultiNested'."""

    __slots__ = [
        '_array_of_arrays',
        '_array_of_bounded_sequences',
        '_array_of_unbounded_sequences',
        '_bounded_sequence_of_arrays',
        '_bounded_sequence_of_bounded_sequences',
        '_bounded_sequence_of_unbounded_sequences',
        '_unbounded_sequence_of_arrays',
        '_unbounded_sequence_of_bounded_sequences',
        '_unbounded_sequence_of_unbounded_sequences',
        '_check_fields',
    ]

    _fields_and_field_types: dict[str, str] = {
        'array_of_arrays': 'test_msgs/Arrays[3]',
        'array_of_bounded_sequences': 'test_msgs/BoundedSequences[3]',
        'array_of_unbounded_sequences': 'test_msgs/UnboundedSequences[3]',
        'bounded_sequence_of_arrays': 'sequence<test_msgs/Arrays, 3>',
        'bounded_sequence_of_bounded_sequences': 'sequence<test_msgs/BoundedSequences, 3>',
        'bounded_sequence_of_unbounded_sequences': 'sequence<test_msgs/UnboundedSequences, 3>',
        'unbounded_sequence_of_arrays': 'sequence<test_msgs/Arrays>',
        'unbounded_sequence_of_bounded_sequences': 'sequence<test_msgs/BoundedSequences>',
        'unbounded_sequence_of_unbounded_sequences': 'sequence<test_msgs/UnboundedSequences>',
    }

    # This attribute is used to store an rosidl_parser.definition variable
    # related to the data type of each of the components the message.
    SLOT_TYPES: tuple[rosidl_parser.definition.AbstractType, ...] = (
        rosidl_parser.definition.Array(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'Arrays'), 3),  # noqa: E501
        rosidl_parser.definition.Array(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'BoundedSequences'), 3),  # noqa: E501
        rosidl_parser.definition.Array(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'UnboundedSequences'), 3),  # noqa: E501
        rosidl_parser.definition.BoundedSequence(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'Arrays'), 3),  # noqa: E501
        rosidl_parser.definition.BoundedSequence(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'BoundedSequences'), 3),  # noqa: E501
        rosidl_parser.definition.BoundedSequence(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'UnboundedSequences'), 3),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'Arrays')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'BoundedSequences')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.NamespacedType(['test_msgs', 'msg'], 'UnboundedSequences')),  # noqa: E501
    )

    def __init__(self, *,
                 array_of_arrays: typing.Optional[collections.abc.Sequence[test_msgs.msg.Arrays]] = None,  # noqa: E501
                 array_of_bounded_sequences: typing.Optional[collections.abc.Sequence[test_msgs.msg.BoundedSequences]] = None,  # noqa: E501
                 array_of_unbounded_sequences: typing.Optional[collections.abc.Sequence[test_msgs.msg.UnboundedSequences]] = None,  # noqa: E501
                 bounded_sequence_of_arrays: typing.Optional[collections.abc.Sequence[test_msgs.msg.Arrays]] = None,  # noqa: E501
                 bounded_sequence_of_bounded_sequences: typing.Optional[collections.abc.Sequence[test_msgs.msg.BoundedSequences]] = None,  # noqa: E501
                 bounded_sequence_of_unbounded_sequences: typing.Optional[collections.abc.Sequence[test_msgs.msg.UnboundedSequences]] = None,  # noqa: E501
                 unbounded_sequence_of_arrays: typing.Optional[collections.abc.Sequence[test_msgs.msg.Arrays]] = None,  # noqa: E501
                 unbounded_sequence_of_bounded_sequences: typing.Optional[collections.abc.Sequence[test_msgs.msg.BoundedSequences]] = None,  # noqa: E501
                 unbounded_sequence_of_unbounded_sequences: typing.Optional[collections.abc.Sequence[test_msgs.msg.UnboundedSequences]] = None,  # noqa: E501
                 check_fields: typing.Optional[bool] = None) -> None:
        if check_fields is not None:
            self._check_fields = check_fields
        else:
            self._check_fields = ros_python_check_fields == '1'
        from test_msgs.msg import Arrays
        self.array_of_arrays = array_of_arrays if array_of_arrays is not None else [Arrays() for x in range(3)]
        from test_msgs.msg import BoundedSequences
        self.array_of_bounded_sequences = array_of_bounded_sequences if array_of_bounded_sequences is not None else [BoundedSequences() for x in range(3)]
        from test_msgs.msg import UnboundedSequences
        self.array_of_unbounded_sequences = array_of_unbounded_sequences if array_of_unbounded_sequences is not None else [UnboundedSequences() for x in range(3)]
        self.bounded_sequence_of_arrays = bounded_sequence_of_arrays if bounded_sequence_of_arrays is not None else []
        self.bounded_sequence_of_bounded_sequences = bounded_sequence_of_bounded_sequences if bounded_sequence_of_bounded_sequences is not None else []
        self.bounded_sequence_of_unbounded_sequences = bounded_sequence_of_unbounded_sequences if bounded_sequence_of_unbounded_sequences is not None else []
        self.unbounded_sequence_of_arrays = unbounded_sequence_of_arrays if unbounded_sequence_of_arrays is not None else []
        self.unbounded_sequence_of_bounded_sequences = unbounded_sequence_of_bounded_sequences if unbounded_sequence_of_bounded_sequences is not None else []
        self.unbounded_sequence_of_unbounded_sequences = unbounded_sequence_of_unbounded_sequences if unbounded_sequence_of_unbounded_sequences is not None else []

    def __repr__(self) -> str:
        typename = self.__class__.__module__.split('.')
        typename.pop()
        typename.append(self.__class__.__name__)
        args: list[str] = []
        for s, t in zip(self.get_fields_and_field_types().keys(), self.SLOT_TYPES):
            field = getattr(self, s)
            fieldstr = repr(field)
            # We use Python array type for fields that can be directly stored
            # in them, and "normal" sequences for everything else.  If it is
            # a type that we store in an array, strip off the 'array' portion.
            if (
                isinstance(t, rosidl_parser.definition.AbstractSequence) and
                isinstance(t.value_type, rosidl_parser.definition.BasicType) and
                t.value_type.typename in ['float', 'double', 'int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32', 'int64', 'uint64']
            ):
                if len(field) == 0:
                    fieldstr = '[]'
                else:
                    from rosidl_buffer import Buffer as _RosidlBuffer
                    if not isinstance(field, _RosidlBuffer):
                        if self._check_fields:
                            assert fieldstr.startswith('array(')
                        prefix = "array('X', "
                        suffix = ')'
                        fieldstr = fieldstr[len(prefix):-len(suffix)]
            args.append(s + '=' + fieldstr)
        return '%s(%s)' % ('.'.join(typename), ', '.join(args))

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, MultiNested):
            return False
        if self.array_of_arrays != other.array_of_arrays:
            return False
        if self.array_of_bounded_sequences != other.array_of_bounded_sequences:
            return False
        if self.array_of_unbounded_sequences != other.array_of_unbounded_sequences:
            return False
        if self.bounded_sequence_of_arrays != other.bounded_sequence_of_arrays:
            return False
        if self.bounded_sequence_of_bounded_sequences != other.bounded_sequence_of_bounded_sequences:
            return False
        if self.bounded_sequence_of_unbounded_sequences != other.bounded_sequence_of_unbounded_sequences:
            return False
        if self.unbounded_sequence_of_arrays != other.unbounded_sequence_of_arrays:
            return False
        if self.unbounded_sequence_of_bounded_sequences != other.unbounded_sequence_of_bounded_sequences:
            return False
        if self.unbounded_sequence_of_unbounded_sequences != other.unbounded_sequence_of_unbounded_sequences:
            return False
        return True

    @classmethod
    def get_fields_and_field_types(cls) -> dict[str, str]:
        from copy import copy
        return copy(cls._fields_and_field_types)

    @builtins.property
    def array_of_arrays(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.Arrays]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'array_of_arrays'."""
        return self._array_of_arrays

    @array_of_arrays.setter
    def array_of_arrays(self, value: collections.abc.Sequence[test_msgs.msg.Arrays]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import Arrays

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     len(value) == 3 and
                     all(isinstance(v, Arrays) for v in value) and
                     True), \
                    "The 'array_of_arrays' field must be sequence with length 3 and each value of type 'Arrays'"

        if isinstance(value, list):
            self._array_of_arrays = value
            return
        self._array_of_arrays = list(value)

    @builtins.property
    def array_of_bounded_sequences(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.BoundedSequences]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'array_of_bounded_sequences'."""
        return self._array_of_bounded_sequences

    @array_of_bounded_sequences.setter
    def array_of_bounded_sequences(self, value: collections.abc.Sequence[test_msgs.msg.BoundedSequences]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import BoundedSequences

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     len(value) == 3 and
                     all(isinstance(v, BoundedSequences) for v in value) and
                     True), \
                    "The 'array_of_bounded_sequences' field must be sequence with length 3 and each value of type 'BoundedSequences'"

        if isinstance(value, list):
            self._array_of_bounded_sequences = value
            return
        self._array_of_bounded_sequences = list(value)

    @builtins.property
    def array_of_unbounded_sequences(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.UnboundedSequences]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'array_of_unbounded_sequences'."""
        return self._array_of_unbounded_sequences

    @array_of_unbounded_sequences.setter
    def array_of_unbounded_sequences(self, value: collections.abc.Sequence[test_msgs.msg.UnboundedSequences]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import UnboundedSequences

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     len(value) == 3 and
                     all(isinstance(v, UnboundedSequences) for v in value) and
                     True), \
                    "The 'array_of_unbounded_sequences' field must be sequence with length 3 and each value of type 'UnboundedSequences'"

        if isinstance(value, list):
            self._array_of_unbounded_sequences = value
            return
        self._array_of_unbounded_sequences = list(value)

    @builtins.property
    def bounded_sequence_of_arrays(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.Arrays]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'bounded_sequence_of_arrays'."""
        return self._bounded_sequence_of_arrays

    @bounded_sequence_of_arrays.setter
    def bounded_sequence_of_arrays(self, value: collections.abc.Sequence[test_msgs.msg.Arrays]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import Arrays

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     len(value) <= 3 and
                     all(isinstance(v, Arrays) for v in value) and
                     True), \
                    "The 'bounded_sequence_of_arrays' field must be sequence with length <= 3 and each value of type 'Arrays'"

        if isinstance(value, list):
            self._bounded_sequence_of_arrays = value
            return
        self._bounded_sequence_of_arrays = list(value)

    @builtins.property
    def bounded_sequence_of_bounded_sequences(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.BoundedSequences]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'bounded_sequence_of_bounded_sequences'."""
        return self._bounded_sequence_of_bounded_sequences

    @bounded_sequence_of_bounded_sequences.setter
    def bounded_sequence_of_bounded_sequences(self, value: collections.abc.Sequence[test_msgs.msg.BoundedSequences]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import BoundedSequences

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     len(value) <= 3 and
                     all(isinstance(v, BoundedSequences) for v in value) and
                     True), \
                    "The 'bounded_sequence_of_bounded_sequences' field must be sequence with length <= 3 and each value of type 'BoundedSequences'"

        if isinstance(value, list):
            self._bounded_sequence_of_bounded_sequences = value
            return
        self._bounded_sequence_of_bounded_sequences = list(value)

    @builtins.property
    def bounded_sequence_of_unbounded_sequences(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.UnboundedSequences]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'bounded_sequence_of_unbounded_sequences'."""
        return self._bounded_sequence_of_unbounded_sequences

    @bounded_sequence_of_unbounded_sequences.setter
    def bounded_sequence_of_unbounded_sequences(self, value: collections.abc.Sequence[test_msgs.msg.UnboundedSequences]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import UnboundedSequences

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     len(value) <= 3 and
                     all(isinstance(v, UnboundedSequences) for v in value) and
                     True), \
                    "The 'bounded_sequence_of_unbounded_sequences' field must be sequence with length <= 3 and each value of type 'UnboundedSequences'"

        if isinstance(value, list):
            self._bounded_sequence_of_unbounded_sequences = value
            return
        self._bounded_sequence_of_unbounded_sequences = list(value)

    @builtins.property
    def unbounded_sequence_of_arrays(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.Arrays]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'unbounded_sequence_of_arrays'."""
        return self._unbounded_sequence_of_arrays

    @unbounded_sequence_of_arrays.setter
    def unbounded_sequence_of_arrays(self, value: collections.abc.Sequence[test_msgs.msg.Arrays]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import Arrays

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     all(isinstance(v, Arrays) for v in value) and
                     True), \
                    "The 'unbounded_sequence_of_arrays' field must be sequence and each value of type 'Arrays'"

        if isinstance(value, list):
            self._unbounded_sequence_of_arrays = value
            return
        self._unbounded_sequence_of_arrays = list(value)

    @builtins.property
    def unbounded_sequence_of_bounded_sequences(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.BoundedSequences]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'unbounded_sequence_of_bounded_sequences'."""
        return self._unbounded_sequence_of_bounded_sequences

    @unbounded_sequence_of_bounded_sequences.setter
    def unbounded_sequence_of_bounded_sequences(self, value: collections.abc.Sequence[test_msgs.msg.BoundedSequences]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import BoundedSequences

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     all(isinstance(v, BoundedSequences) for v in value) and
                     True), \
                    "The 'unbounded_sequence_of_bounded_sequences' field must be sequence and each value of type 'BoundedSequences'"

        if isinstance(value, list):
            self._unbounded_sequence_of_bounded_sequences = value
            return
        self._unbounded_sequence_of_bounded_sequences = list(value)

    @builtins.property
    def unbounded_sequence_of_unbounded_sequences(self) -> typing.Annotated[typing.Any, list[test_msgs.msg.UnboundedSequences]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'unbounded_sequence_of_unbounded_sequences'."""
        return self._unbounded_sequence_of_unbounded_sequences

    @unbounded_sequence_of_unbounded_sequences.setter
    def unbounded_sequence_of_unbounded_sequences(self, value: collections.abc.Sequence[test_msgs.msg.UnboundedSequences]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from test_msgs.msg import UnboundedSequences

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     all(isinstance(v, UnboundedSequences) for v in value) and
                     True), \
                    "The 'unbounded_sequence_of_unbounded_sequences' field must be sequence and each value of type 'UnboundedSequences'"

        if isinstance(value, list):
            self._unbounded_sequence_of_unbounded_sequences = value
            return
        self._unbounded_sequence_of_unbounded_sequences = list(value)
