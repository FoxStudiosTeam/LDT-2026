# generated from rosidl_generator_py/resource/_idl.py.em
# with input from rmw_dds_common:msg\NodeEntitiesInfo.idl
# generated code does not contain a copyright notice

from __future__ import annotations

import collections.abc
import os
import typing

import rosidl_pycommon.interface_base_classes

if typing.TYPE_CHECKING:
    from ctypes import Structure

    class PyCapsule(Structure):
        pass  # don't need to define the full structure


# This is being done at the module level and not on the instance level to avoid looking
# for the same variable multiple times on each instance. This variable is not supposed to
# change during runtime so it makes sense to only look for it once.
ros_python_check_fields = os.getenv('ROS_PYTHON_CHECK_FIELDS', default='')


if typing.TYPE_CHECKING:
    import collections  # noqa: E402, I100, I201, I300
    import rmw_dds_common.msg  # noqa: E402, I100, I201, I300


# Import statements for member types

import builtins  # noqa: E402, I100

import rosidl_parser.definition  # noqa: E402, I100


class Metaclass_NodeEntitiesInfo(rosidl_pycommon.interface_base_classes.MessageTypeSupportMeta):
    """Metaclass of message 'NodeEntitiesInfo'."""

    _CREATE_ROS_MESSAGE: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _CONVERT_FROM_PY: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _CONVERT_TO_PY: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _DESTROY_ROS_MESSAGE: typing.ClassVar[typing.Optional[PyCapsule]] = None
    _TYPE_SUPPORT: typing.ClassVar[typing.Optional[PyCapsule]] = None

    class NodeEntitiesInfoConstants(typing.TypedDict):
        pass

    __constants: NodeEntitiesInfoConstants = {
    }

    @classmethod
    def __import_type_support__(cls) -> None:
        try:
            from rosidl_generator_py import import_type_support  # type: ignore[attr-defined]
            module = import_type_support('rmw_dds_common')
        except ImportError:
            import logging
            import traceback
            logger = logging.getLogger(
                'rmw_dds_common.msg.NodeEntitiesInfo')
            logger.debug(
                'Failed to import needed modules for type support:\n' +
                traceback.format_exc())
        else:
            cls._CREATE_ROS_MESSAGE = module.create_ros_message_msg__msg__node_entities_info
            cls._CONVERT_FROM_PY = module.convert_from_py_msg__msg__node_entities_info
            cls._CONVERT_TO_PY = module.convert_to_py_msg__msg__node_entities_info
            cls._TYPE_SUPPORT = module.type_support_msg__msg__node_entities_info
            cls._DESTROY_ROS_MESSAGE = module.destroy_ros_message_msg__msg__node_entities_info

            from rmw_dds_common.msg import Gid
            if Gid._TYPE_SUPPORT is None:
                Gid.__import_type_support__()

    @classmethod
    def __prepare__(metacls, name: str, bases: tuple[type[typing.Any], ...], /, **kwds: typing.Any) -> collections.abc.MutableMapping[str, object]:
        # list constant names here so that they appear in the help text of
        # the message class under "Data and other attributes defined here:"
        # as well as populate each message instance
        return {
        }


class NodeEntitiesInfo(rosidl_pycommon.interface_base_classes.BaseMessage, metaclass=Metaclass_NodeEntitiesInfo):
    """Message class 'NodeEntitiesInfo'."""

    __slots__ = [
        '_node_namespace',
        '_node_name',
        '_reader_gid_seq',
        '_writer_gid_seq',
        '_check_fields',
    ]

    _fields_and_field_types: dict[str, str] = {
        'node_namespace': 'string<256>',
        'node_name': 'string<256>',
        'reader_gid_seq': 'sequence<rmw_dds_common/Gid>',
        'writer_gid_seq': 'sequence<rmw_dds_common/Gid>',
    }

    # This attribute is used to store an rosidl_parser.definition variable
    # related to the data type of each of the components the message.
    SLOT_TYPES: tuple[rosidl_parser.definition.AbstractType, ...] = (
        rosidl_parser.definition.BoundedString(256),  # noqa: E501
        rosidl_parser.definition.BoundedString(256),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.NamespacedType(['rmw_dds_common', 'msg'], 'Gid')),  # noqa: E501
        rosidl_parser.definition.UnboundedSequence(rosidl_parser.definition.NamespacedType(['rmw_dds_common', 'msg'], 'Gid')),  # noqa: E501
    )

    def __init__(self, *,
                 node_namespace: typing.Optional[typing.Union[str, collections.UserString]] = None,  # noqa: E501
                 node_name: typing.Optional[typing.Union[str, collections.UserString]] = None,  # noqa: E501
                 reader_gid_seq: typing.Optional[collections.abc.Sequence[rmw_dds_common.msg.Gid]] = None,  # noqa: E501
                 writer_gid_seq: typing.Optional[collections.abc.Sequence[rmw_dds_common.msg.Gid]] = None,  # noqa: E501
                 check_fields: typing.Optional[bool] = None) -> None:
        if check_fields is not None:
            self._check_fields = check_fields
        else:
            self._check_fields = ros_python_check_fields == '1'
        self.node_namespace = node_namespace if node_namespace is not None else str()
        self.node_name = node_name if node_name is not None else str()
        self.reader_gid_seq = reader_gid_seq if reader_gid_seq is not None else []
        self.writer_gid_seq = writer_gid_seq if writer_gid_seq is not None else []

    def __repr__(self) -> str:
        typename = self.__class__.__module__.split('.')
        typename.pop()
        typename.append(self.__class__.__name__)
        args: list[str] = []
        for s, t in zip(self.get_fields_and_field_types().keys(), self.SLOT_TYPES):
            field = getattr(self, s)
            fieldstr = repr(field)
            # We use Python array type for fields that can be directly stored
            # in them, and "normal" sequences for everything else.  If it is
            # a type that we store in an array, strip off the 'array' portion.
            if (
                isinstance(t, rosidl_parser.definition.AbstractSequence) and
                isinstance(t.value_type, rosidl_parser.definition.BasicType) and
                t.value_type.typename in ['float', 'double', 'int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32', 'int64', 'uint64']
            ):
                if len(field) == 0:
                    fieldstr = '[]'
                else:
                    from rosidl_buffer import Buffer as _RosidlBuffer
                    if not isinstance(field, _RosidlBuffer):
                        if self._check_fields:
                            assert fieldstr.startswith('array(')
                        prefix = "array('X', "
                        suffix = ')'
                        fieldstr = fieldstr[len(prefix):-len(suffix)]
            args.append(s + '=' + fieldstr)
        return '%s(%s)' % ('.'.join(typename), ', '.join(args))

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, NodeEntitiesInfo):
            return False
        if self.node_namespace != other.node_namespace:
            return False
        if self.node_name != other.node_name:
            return False
        if self.reader_gid_seq != other.reader_gid_seq:
            return False
        if self.writer_gid_seq != other.writer_gid_seq:
            return False
        return True

    @classmethod
    def get_fields_and_field_types(cls) -> dict[str, str]:
        from copy import copy
        return copy(cls._fields_and_field_types)

    @builtins.property
    def node_namespace(self) -> typing.Union[str, collections.UserString]:
        """Message field 'node_namespace'."""
        return self._node_namespace

    @node_namespace.setter
    def node_namespace(self, value: typing.Union[str, collections.UserString]) -> None:

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    (isinstance(value, (str, collections.UserString)) and
                     len(value) <= 256), \
                    "The 'node_namespace' field must be string value " \
                    'not longer than 256'

        self._node_namespace = value

    @builtins.property
    def node_name(self) -> typing.Union[str, collections.UserString]:
        """Message field 'node_name'."""
        return self._node_name

    @node_name.setter
    def node_name(self, value: typing.Union[str, collections.UserString]) -> None:

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    (isinstance(value, (str, collections.UserString)) and
                     len(value) <= 256), \
                    "The 'node_name' field must be string value " \
                    'not longer than 256'

        self._node_name = value

    @builtins.property
    def reader_gid_seq(self) -> typing.Annotated[typing.Any, list[rmw_dds_common.msg.Gid]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'reader_gid_seq'."""
        return self._reader_gid_seq

    @reader_gid_seq.setter
    def reader_gid_seq(self, value: collections.abc.Sequence[rmw_dds_common.msg.Gid]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from rmw_dds_common.msg import Gid

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     all(isinstance(v, Gid) for v in value) and
                     True), \
                    "The 'reader_gid_seq' field must be sequence and each value of type 'Gid'"

        if isinstance(value, list):
            self._reader_gid_seq = value
            return
        self._reader_gid_seq = list(value)

    @builtins.property
    def writer_gid_seq(self) -> typing.Annotated[typing.Any, list[rmw_dds_common.msg.Gid]]:   # typing.Annotated can be remove after mypy 1.16+ see mypy#3004
        """Message field 'writer_gid_seq'."""
        return self._writer_gid_seq

    @writer_gid_seq.setter
    def writer_gid_seq(self, value: collections.abc.Sequence[rmw_dds_common.msg.Gid]) -> None:
        if isinstance(value, collections.abc.Set):
            import warnings
            warnings.warn(
                'Using set or subclass of set is deprecated,'
                ' please use a subclass of collections.abc.Sequence like list',
                DeprecationWarning)
        from rmw_dds_common.msg import Gid

        if self._check_fields:
            if False:  # Done for templating alignment
                pass
            else:
                assert \
                    ((isinstance(value, collections.abc.Sequence) or
                     isinstance(value, collections.abc.Set)) and
                     not isinstance(value, str) and
                     not isinstance(value, collections.UserString) and
                     all(isinstance(v, Gid) for v in value) and
                     True), \
                    "The 'writer_gid_seq' field must be sequence and each value of type 'Gid'"

        if isinstance(value, list):
            self._writer_gid_seq = value
            return
        self._writer_gid_seq = list(value)
