# Windows
Скачать или скомпилировать rerun из исходников или релизов на github и добавить в PATH
```ros2/local_setup.ps1```

```python -m venv venv --system-site-packages```

```.\venv\Scripts\activate```

```pip install -r req```

``` python rerun_viewer.py```